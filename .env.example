# Fortunexdigital — environment variables (example)
# Copy this file to .env and fill in your values.
# Never commit the real .env to version control.

SITE_URL=https://fortunexdigitals.com

DB_HOST=localhost
DB_NAME=fortunexdigital
DB_USERNAME=root
DB_PORT=3306
DB_PASSWORD=
